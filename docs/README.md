# Draughts — Documentation

- **[System Architecture](architecture/README.md)** — version 1.4, split into one file per section with an index.

`docs/architecture/` is the only authoritative copy. Earlier single-file revisions are not kept in the repository; use `git log` on this directory for history.
